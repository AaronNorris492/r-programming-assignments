library(plyr)

grades_data <- read.table(file.choose(), header = TRUE, sep = ",", stringsAsFactors = FALSE)
str(grades_data)

gender_mean <- ddply(
  grades_data,
  "Sex",
  summarise,
  GradeAverage = mean(Grade, na.rm = TRUE)
)

print(gender_mean)

write.table(
  gender_mean,
  file = "gender_mean.txt",
  sep = "\t",
  row.names = FALSE
)

i_students <- subset(
  grades_data,
  grepl("i", Name, ignore.case = TRUE)
)

print(i_students)

write.csv(
  i_students$Name,
  file = "i_students.csv",
  row.names = FALSE
)
